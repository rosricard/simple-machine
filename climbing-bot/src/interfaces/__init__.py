"""Contracts shared across the stack: geometry, command/event schemas, enums,
fault types.

In production these would live in a separate interface repo (e.g. a `.proto`
package) imported by both the bot firmware and the orchestration system. Kept
in-tree here for the sample.
"""
